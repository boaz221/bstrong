import React from 'react';
import PropTypes from 'prop-types';

function MyArea(props) {
    return (
        <div>MyArea</div>
    );
}

MyArea.propTypes = {};
MyArea.defaultProps = {};

export default MyArea;
